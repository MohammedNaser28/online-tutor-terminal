run:
  timeout: 5m

linters:
  disable:
    - errcheck
    - unused
    - ineffassign
    - gosimple

issues:
  exclude-rules:
    - path: "_test\\.go"
      linters:
        - errcheck
        - vet
